export enum HttpStatusCode {
  OK = 200,
  CREATED = 201,
  FORBIDEN = 403,
  BAD_REQUEST = 400,
  NOT_FOUND = 404,
}

export enum HttpStatusText {
  OK = 'ok',
  CREATED = 'created',
  FORBIDEN = 'forbiden',
  BAD_REQUEST = 'bad request',
  NOT_FOUND = 'not found',
}
